#include <stdio.h>
int main(){
    int a , b ;
    printf("Enter a, b : ");
    scanf("%d %d ", &a , &b );
    
    if ( a == b){
        printf(" The number a and b are equal ");
    }
    else
        {
            printf(" The number a and b are NOT equal");
        }
}
